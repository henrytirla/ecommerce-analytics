.evidence/template
.svelte-kit
# build/ is intentionally tracked for static (snapshot) hosting on Vercel
# build
node_modules
.DS_Store
# static/data contains the generated snapshot data for this portfolio deployment
# (so Vercel can build without database credentials)
# static/data
*.options.yaml
.vscode/settings.json
.env
.evidence/meta
